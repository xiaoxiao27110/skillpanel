# SkillPanel

SkillPanel 是共享 OpenCode/Hermes skill 的开关面板:VS Code 扩展通过本机控制器管理 skill 的启停,场景是其批量切换的单位。

## Language

**Skill**:
一个含 `SKILL.md` 的目录,开关的最小单位;开/关即目录在启用区与停用区之间搬动。

**场景 (Scene)**:
一组 skill 开关状态的命名预设,批量切换的单位。场景的载体是关名单。
_Avoid_: profile、preset、配置组

**关名单 (Deny-list)**:
场景记录的一组「要关闭」的 skill 名字。apply 场景 = 名单内关闭、名单外一律打开;未被场景记录的 skill 天然是开的。
_Avoid_: 黑名单、禁用列表

**激活场景 (Active scene)**:
当前唯一生效的场景,任何时刻恰有一个;它决定手动开关写回何处。不存在「无场景」状态。
_Avoid_: 当前场景、选中场景

**写回 (Write-back)**:
激活场景下的手动开关立即并入该场景关名单的规则:关 → 加入名单,开 → 移出名单。手动微调不是临时态,而是对场景的编辑。

**预设场景 (Seeded scene)**:
随部署(Docker 配置)首次启动时写入的场景,与用户自建场景无任何区别;插件本身不含任何场景定义。
_Avoid_: 内置场景、出厂场景

**默认场景 (Default scene)**:
首次启动自动创建的兜底场景,接管当时的开关状态;删光所有场景时重新生成,关名单为空。
