diff --git a/hermes_cli/main.py b/hermes_cli/main.py
--- a/hermes_cli/main.py
+++ b/hermes_cli/main.py
@@ -1747,7 +1747,10 @@ def _make_tui_argv(tui_dir: Path, tui_dev: bool) -> tuple[list[str], Path]:
         )
         sys.exit(1)
 
-    if not ext_dir:
+    # A wheel ships a ready-to-run hermes_cli/tui_dist/entry.js but not the
+    # source ui-tui workspace. Only require that workspace when no bundled
+    # production TUI is available (or when --dev explicitly needs sources).
+    if not ext_dir and (tui_dev or _find_bundled_tui() is None):
         _ensure_tui_workspace(tui_dir)
 
     # 1. Prebuilt bundle (nix / packaged release): just run it.
